/**
 * appCommonDirectivesUtilitiesDraggable Class
 *
 * Makes DOM element draggable within the containing DIV
 *
 * Dependency Injections:
 * @param {object}  $document   reference to AngularJS $document object utilized for capturing DOM events
 *
 * @returns {appCommonDirectivesUtilitiesDraggable}
 *
 **/

/**
 * DETAILED SPECIFICATION
 *
 * This allows users to move elements around within the containing DIV.  This may be because the element is blocking
 * view.  Restrict linkage of this directive to the attribute of the target DOM element.  The HTML tag must be a
 * hyphenated version of the class name.
 *
 **/
 